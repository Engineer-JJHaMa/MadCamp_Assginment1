import 'package:flutter/material.dart';

class TmpTab extends StatelessWidget {
  const TmpTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(backgroundColor: Colors.grey, body: Center());
  }
}
